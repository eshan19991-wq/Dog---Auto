
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:label="Dog Auto" android:icon="@mipmap/ic_launcher">
        <service
            android:name=".CarService"
            android:permission="androidx.car.app.permission.CAR_APP">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
                <category android:name="androidx.car.app.category.MESSAGING" />
            </intent-filter>
            <meta-data android:name="androidx.car.app.version" android:value="1.3.0" />
        </service>
    </application>

</manifest>
